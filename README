example from book "C++ GUI Programming with Qt4"

qmake -project	//to generate the .pro file
qmake			//to generate Makefile
make			//to generate executive file


Error when 'make':
	QApplication: No such file or directory
Solution:
	sudo apt-get install qt4-dev-tools
